version: '3.8'

services:
  api:
    build: .
    container_name: rag_api
    restart: always
    ports:
      - "8000:8000"
    environment:
      # Explicitly tell the API to look for qdrant at the container name instead of localhost
      - QDRANT_HOST=qdrant
      # Pass the Groq API key from the host environment to the container
      - GROQ_API_KEY=${GROQ_API_KEY}
    depends_on:
      - qdrant
    deploy:
      resources:
        limits:
          cpus: '3.0' # Reserve ~1 core for Qdrant/OS on a 4-core instance
          memory: 4G  # Limit RAM to prevent EC2 OOM

  qdrant:
    image: qdrant/qdrant:latest
    restart: always
    container_name: qdrant_prod
    ports:
      - "6333:6333" # REST API
      - "6334:6334" # gRPC API
    volumes:
      - ./qdrant_data:/qdrant/storage
    environment:
      - QDRANT__SERVICE__MAX_REQUEST_SIZE_MB=50
    ulimits:
      nofile:
        soft: 65535
        hard: 65535
    deploy:
      resources:
        limits:
          cpus: '1.0'
          memory: 2G
